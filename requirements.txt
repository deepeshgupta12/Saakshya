# Saakshya local-first (M1 Mac, arm64 wheels, NO TA-Lib) — SPEC §12 / docs/27 §7.3
# Core data + storage (polyglot: TimescaleDB + MongoDB, D-059)
psycopg[binary]==3.2.3    # TimescaleDB/Postgres driver (analytics core)
pymongo==4.10.1           # MongoDB driver (user/app documents)
pandas==2.2.3
numpy==2.1.3
scipy==1.14.1            # M3b: Spearman IC computation (spearmanr)
# EOD market data — Zerodha Kite Connect is the sole source (D-058, SPEC §8).
kiteconnect==5.0.1       # official Zerodha SDK: instruments + historical_data
pyotp==2.9.0             # automated TOTP 2FA for unattended daily login
# Config + models
pydantic==2.10.4
pydantic-settings==2.7.0
# API (used from M5; pinned now so `pip install -r` is complete)
fastapi==0.115.6
uvicorn==0.34.0
httpx==0.28.1
# AI layer (used from M4)
anthropic==0.42.0
# Tooling / tests
pytest==8.3.4
mypy==1.14.1
ruff==0.8.6
